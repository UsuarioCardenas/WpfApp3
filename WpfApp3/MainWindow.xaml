<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sistema de Transporte - Login" Height="400" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#f0f0f0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="SISTEMA DE TRANSPORTE" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#333"/>

            <TextBlock Text="Login" FontSize="18" FontWeight="SemiBold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#555"/>

            <TextBlock Text="Usuario:" Margin="0,10,0,5" FontWeight="Medium"/>
            <TextBox x:Name="txtUsuario" Height="35" FontSize="14" Padding="10,0"/>

            <TextBlock Text="Contraseña:" Margin="0,15,0,5" FontWeight="Medium"/>
            <PasswordBox x:Name="txtPassword" Height="35" FontSize="14" Padding="10,0"/>

            <Button x:Name="btnLogin" Content="Iniciar Sesión" Height="40" Margin="0,25,0,0"
                    Background="#4CAF50" Foreground="White" FontSize="14" FontWeight="Medium"
                    Click="BtnLogin_Click"/>

            <TextBlock x:Name="lblMensaje" Margin="0,15,0,0" HorizontalAlignment="Center"
                       FontSize="12" Foreground="Red" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
